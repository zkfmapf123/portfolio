export type StatisticDto = {
    prevDate :string;
    nextDate :string;
};

export type StatisticStudyDto = {
    date : string;
};

export type WeekGraphType = {
    sum : string;
    date: string;
};

export type WeekTotalType = {
    
}

