import { Dimensions } from "react-native";

export const DEVISE_WIDTH = Math.round(Dimensions.get("screen").width);
export const DEVISE_HEIGHT = Math.round(Dimensions.get("screen").height);
export const MARGIN_AUTO = {
  marginTop: 0,
  marginRight: 'auto',
  marginBottom: 0,
  marginLeft : 'auto'
}