import { IdType } from "../../Apis/Services/recipe/recipe.dto";
import { AppServices } from "./AppServices";

export class Recipe extends AppServices{
 
  // recipe 만들기(create)
  protected createRecipe = () => { };
  
  // recipe doc하나 가져오기(read)
  protected getRecipe = () => { };

  // update
  protected updateRecipe = () => { };

  // delete
  protected deleteRecipe = () => { };
}